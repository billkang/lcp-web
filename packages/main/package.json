{
  "name": "@lcp/main",
  "version": "0.0.1",
  "description": "门户站点，整个项目的基座",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "vue-tsc --noEmit && pnpm lint:eslint && pnpm lint:stylelint"
  }
}
